name: Procesar Facturas — Empresa 2 Carmen Fortes

on:
  push:
    paths:
      - 'empresa-2-carmen-fortes/facturas/nuevas/**'

jobs:
  procesar:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar Tesseract OCR y poppler
        run: sudo apt-get install -y tesseract-ocr tesseract-ocr-spa poppler-utils libheif-dev

      - name: Instalar dependencias Python
        run: pip install pdfplumber pdf2image pillow pillow-heif pytesseract google-genai

      - name: Procesar facturas nuevas
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python empresa-2-carmen-fortes/procesar_gha.py

      - name: Validar JSON antes de subir
        run: python -c "import json; json.load(open('empresa-2-carmen-fortes/facturas_datos.json')); print('JSON válido ✓')"

      - name: Subir cambios al repositorio
        run: |
          git config user.name "Dashboard Bot"
          git config user.email "bot@dashboard-facturas"
          git add empresa-2-carmen-fortes/facturas_datos.json empresa-2-carmen-fortes/facturas/
          git diff --staged --quiet || git commit -m "Carmen Fortes: factura procesada automaticamente [skip ci]"
          git pull --rebase origin main
          git push